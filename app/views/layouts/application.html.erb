<!DOCTYPE html>
<html>
  <head>
    <title>TowersDefense</title>
    <link href="https://fonts.googleapis.com/css?family=Audiowide" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css?family=Black+Ops+One" rel="stylesheet">
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
    <script src="//cdn.jsdelivr.net/npm/phaser@3.0.0/dist/phaser.min.js"></script>
  </head>

  <body>
   
    <nav>
      <ul class="navbar">
        <li id="play" class="navbar_item"><%= link_to "Play", game_index_path %> </li>
        <li class="navbar_item"><a href="#instructions">Info</a></li>
        <% if !user_signed_in? %>
        <li class="navbar_item"><%= link_to "Log In", '/'%></li>
        <% end %>
        <% if user_signed_in? %>
        <li class="navbar_item"><%= link_to "Sign Out", destroy_user_session_path, method: :delete %></li>
        <li class="navbar_item"><%= link_to "Settings",  edit_user_registration_path %></li>
        <% end %>
      </ul>
    </nav>
    <h1 id="game-title">Tower defenders</h1>
    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>
    <%= yield %>
  </body>
</html>
